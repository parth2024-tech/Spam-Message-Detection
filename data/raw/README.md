# data/raw

Place the dataset CSV here for local runs.

Expected filename:

```text
SPAM text message 20170820 - Data.csv
```

The notebook can also load the file from:

- the repository root
- `sms_data/`
- the standard Kaggle input path

Raw dataset files are ignored by Git.
